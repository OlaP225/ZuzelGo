# Speedway Game

Speedway Game is an iOS application in active development for speedway fans. The current version includes Firebase-based authentication with dedicated login and registration views. Users can rank riders based on predicted points and vote according to expected race results.

The application is built using the MVVM architecture. The project is continuously expanded with new features and UI improvements.
